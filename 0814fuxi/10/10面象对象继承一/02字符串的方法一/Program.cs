using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Diagnostics;

namespace _02字符串的方法一
{
    class Program
    {
        static void Main(string[] args)
        {
            //string s = "abcdefg";
            ////s[0] = 'b';  //不能这样做，因为是只读的
            ////首先将字符串转换为char类型数组
            //char[] chs = s.ToCharArray();
            //chs[0]='b';
            ////将字符数组转为字符串
            //s = new string(chs);
            ////可以将string看做char类型的只读数组
            //Console.WriteLine(s);
            //Console.ReadKey();

            //StringBuilder sb = new StringBuilder();
            //string str = null;

            ////创建计时器
            //Stopwatch sw=new Stopwatch();
            //sw.Start();
            //for (int i = 0; i < 10000; i++)
            //{
            //    str += i;
            //    sb.Append(i);
            //}
            //sw.Stop();
            //Console.WriteLine(sb.ToString());
            //Console.WriteLine(sw.Elapsed);
            //Console.ReadKey();


        }
    }
}
